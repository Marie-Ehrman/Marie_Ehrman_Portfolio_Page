extends layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Home
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9
        
        a(href=`${project.live_link}` target="_blank")
          h1 #{project.project_name}

        p.lead.text-light
          | #{project.description}

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies

        each technology in project.technologies
          ul
            li= technology

        a.button.expanded(href=`${project.live_link}` target="_blank") Live Demo
        a.button.expanded(href=`${project.github_link}` target="_blank") GitHub Repo
    
    img.image(src=`/static/${project.image_urls[1]}` width="1200" height="550")
    img.image(src=`/static/${project.image_urls[2]}` width="1200" height="550")
    img.image(src=`/static/${project.image_urls[3]}` width="1200" height="550")
    //- img.image(src=`/static/${project.image_urls[4]}` width="1200" height="550")